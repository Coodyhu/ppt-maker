{
  "name": "ppt-maker",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter './packages/web' dev",
    "build": "bun run --filter '*' build",
    "cli": "bun run packages/cli/src/index.ts",
    "example:editable": "bun run examples/editable-pptx.ts"
  },
  "dependencies": {
    "pptxgenjs": "^3.12.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/bun": "latest"
  }
}
